#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Give me an expression

    //hold this expression in an array - named expr

    // make new expression - in new_expr
    // for length of expr new_expr [] = expr [], after that new_exp [length+i] = exp [length-i], i++

    //print new exp


    return 0;
}
